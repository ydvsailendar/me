import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: "/", // Change this to your repository name if you're using GitHub Pages, e.g., "/portfolio"
});
